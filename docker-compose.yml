services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./backend/cache:/app/backend/cache
      - ./backend/uploads:/app/backend/uploads
    environment:
      - IMAGE_PIPES_CACHE_DIR=/app/backend/cache
