# Image Pipeline Playground

Visual OpenCV pipeline builder with a React + Vite frontend and FastAPI backend.

## Quick start

See upcoming docs for local development (`uv` + `npm`) and Docker usage.
